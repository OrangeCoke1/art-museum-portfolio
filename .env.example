# Vercel → Project → Settings → Environment Variables
# 本地测试：复制为 .env.local，运行 npx vercel dev

# 必填：Resend Dashboard → API Keys
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxx

# 推荐：已验证域名下的发件地址（未设置则只能用 onboarding@resend.dev 测自己邮箱）
RESEND_FROM=Gallery Walk <hello@your-domain.com>
